# ============================================================
# Webhook (env vars do projeto Vercel, configuradas no import)
# ============================================================
# Gerado pelo Hermes na fase 2 (openssl rand -hex 16). Vai também na URL
# registrada na uazapi (?secret=...)
UAZAPI_WEBHOOK_SECRET=
# Número do dono com DDI, só dígitos (detecta menção/reply em grupo)
OWNER_JID=5511999998888
# Supabase > Project Settings > Data API > Project URL
SUPABASE_URL=
# Supabase > Project Settings > API Keys > service_role (Reveal). NUNCA a anon.
SUPABASE_SERVICE_ROLE_KEY=
# (Opcional, fase 3C) Gerado pelo Hermes (openssl rand -hex 16); vai também
# na WEBHOOK_URL dentro do google-sync/Code.gs
GOOGLE_SYNC_SECRET=
# (Opcional, fase 3D — transcrição de áudio)
# console.groq.com > API Keys (conta do próprio dono, nunca emprestada)
GROQ_API_KEY=
# https://SEUSUBDOMINIO.uazapi.com (mesma da fase 3)
UAZAPI_BASE_URL=
# token da INSTÂNCIA uazapi (o mesmo usado pra registrar o webhook)
UAZAPI_TOKEN=
# Gerado pelo Hermes (openssl rand -hex 16); protege o disparo manual da vassoura
CRON_SECRET=

# ============================================================
# Fireflies-webhook (env vars do 2º projeto Vercel — fase 3B, opcional)
# ============================================================
# Fireflies > Settings > Developer settings > API key
FIREFLIES_API_KEY=
# Gerado pelo Hermes: PRECISA ter 16-32 caracteres (openssl rand -hex 12)
FIREFLIES_WEBHOOK_SECRET=
# Gerado pelo Hermes (openssl rand -hex 16); protege o cron fetch-actions
CRON_SECRET=
# + SUPABASE_URL e SUPABASE_SERVICE_ROLE_KEY (mesmas de cima)

# ============================================================
# Hermes (ambiente do processo que roda agente/secretario.mjs)
# Usa APENAS estas duas (as mesmas SUPABASE_* acima):
#   SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY
# Opcional:
# ============================================================
# Janela de assentamento em minutos: conversa com mensagem mais nova que
# isso espera a próxima varredura (a pessoa pode ainda estar digitando)
SETTLE_MINUTES=10
